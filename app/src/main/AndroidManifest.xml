<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        tools:targetApi="35">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity android:name=".LoginActivity" android:exported="true" />

        <activity android:name=".CreateAccountActivity" android:exported="true" />

        <activity android:name=".DashboardActivity" android:exported="true" />

        <activity android:name=".SectionUnderDevActivity" android:exported="true" />

        <activity android:name=".IndoorPlantsActivity" android:exported="true" />

        <activity android:name=".OutdoorPlantsActivity" android:exported="true" />

        <activity android:name=".SucculentPlantsActivity" android:exported="true" />

        <activity android:name=".HerbPlantsActivity" android:exported="true" />

        <activity android:name=".JournalActivity" android:exported="true" />

        <activity android:name=".ProfileSettingsActivity" android:exported="true" />

        <activity android:name=".PlantDetailActivity" android:exported="true" />

        <activity android:name=".SearchActivity" android:exported="true" />

        <activity android:name=".QnaActivity" android:exported="true" />

        <activity android:name=".FaqActivity" android:exported="true" />

        <activity android:name=".LiveChatActivity" android:exported="true" />

        <activity android:name=".CalendarActivity" android:exported="true" />
    </application>


</manifest>